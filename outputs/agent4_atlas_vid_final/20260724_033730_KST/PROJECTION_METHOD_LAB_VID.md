# Projection Method Lab VID

Implemented route: `/method/projection`.

The Lab explains projection choice without changing the public canonical view. Its tab system offers UMAP 2D, PCA↔UMAP compare, PCA 2D, optional 3D shell, and Tensor specification. Arrow keys, Home, and End move between tabs.

- UMAP 2D consumes the current approved Atlas bundle and existing semantic node core.
- PCA and Tensor display explicit unavailable/specification states because approved payloads are absent.
- The 3D shell contains fixed front/side/top/reset controls, disabled centroid/slice controls, no WebGL data cloud, and an equivalent 2D path.
- Mobile defaults to UMAP and does not promote 3D.
- No projection coordinate is calculated in the browser.

Gate: `PROJECTION_METHOD_LAB_VID_PASS`.
